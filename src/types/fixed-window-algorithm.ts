import type {
  RateLimitConfig,
  RateLimitPolicy,
} from "./bucket-algorithm.js";

export type FixedWindowRateLimitPolicy = RateLimitPolicy;

export type FixedWindowRateLimitConfig = RateLimitConfig;
